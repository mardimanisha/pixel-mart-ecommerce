import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['encrypted-tbn2.gstatic.com','media-photos.depop.com','encrypted-tbn0.gstatic.com','images.pexels.com', 'media.licdn.com', 'i.pinimg.com'],
  }
};

export default nextConfig;
